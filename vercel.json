{
  "version": 2,
  "buildCommand": "npm install --include=dev && prisma generate && tsc",
  "outputDirectory": ".",
  "env": {
    "DATABASE_URL": "file:/tmp/dev.db",
    "JWT_SECRET": "finance-dashboard-secret-2026"
  },
  "functions": {
    "dist/app.js": {
      "runtime": "@vercel/node"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/dist/app.js"
    }
  ]
}
